<p id="notice"><%= notice %></p>
<% @titre_page = 'Liste' %>
<h1 class="text-center">Liste des événements </h1>

<div class="container">
<div class="row">
  <div class="col-lg-8 col-lg-offset-2">
  <!-- Table -->
<table class="table">
  <thead>
    <tr>
      <th>Date</th>
      <th>Titre</th>
      <th>Lieu</th>
      <th>Prix</th>
      <th>Tarif réd.</th>
    </tr>
  </thead>

  <tbody>
    <% @events = @events.sort_by { |e| e.debut} %>
    <% @events.each do |event| %>
      <tr >
        <td><%= event.debut_affiche %></td>
        <td><%= link_to event.titre, event %></td>
        <td><%= event.lieu %></td>
        <td><%= event.prix %></td>
        <td><%= event.reduit ? "Oui" : "Non" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>


<br>

<p class="text-center">
<%= link_to 'Ajouter un événement', new_event_path , class: "btn btn-primary"%>
</p>

